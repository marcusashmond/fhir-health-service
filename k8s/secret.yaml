# Template only — placeholder values, not real credentials.
#
# In a real deployment, don't hand-edit and apply this file as-is. Generate
# it from your secret manager instead, e.g.:
#   kubectl create secret generic fhir-health-service-secrets \
#     --from-literal=DATABASE_URL='postgres://user:pass@rds-host:5432/fhir?sslmode=require' \
#     --from-literal=KAFKA_BROKER='b-1.mskcluster.kafka.us-east-1.amazonaws.com:9092'
# or manage it via a tool like External Secrets Operator / Sealed Secrets so
# the actual values never land in git.
apiVersion: v1
kind: Secret
metadata:
  name: fhir-health-service-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgres://user:password@postgres-host:5432/fhir?sslmode=require"
  KAFKA_BROKER: "kafka-broker:9092"
